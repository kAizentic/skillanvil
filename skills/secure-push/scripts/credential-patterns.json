{
  "_note": "AUTHORITATIVE credential pattern set. Lifted from secure-push/SKILL.md 'Scan reference' on 2026-09-20 so the patterns have ONE home; SKILL.md now points here rather than restating them. Adding a pattern here changes both the human procedure and credential-scan.mjs. Do not re-inline them in prose — a second copy is the mirror-drift this vault lints for.",
  "_severity": "hard = a match blocks. soft = a match is reported and needs a human call (PII), never an unattended block.",
  "patterns": [
    {
      "name": "github-pat-classic",
      "severity": "hard",
      "re": "ghp_[A-Za-z0-9]{36}"
    },
    {
      "name": "github-pat-fine",
      "severity": "hard",
      "re": "github_pat_[A-Za-z0-9_]{22,}"
    },
    {
      "name": "openai-key",
      "severity": "hard",
      "re": "sk-[A-Za-z0-9]{20,}"
    },
    {
      "name": "anthropic-key",
      "severity": "hard",
      "re": "sk-ant-[A-Za-z0-9-]{20,}"
    },
    {
      "name": "aws-access-key",
      "severity": "hard",
      "re": "AKIA[0-9A-Z]{16}"
    },
    {
      "name": "google-api-key",
      "severity": "hard",
      "re": "AIza[0-9A-Za-z_-]{35}"
    },
    {
      "name": "slack-token",
      "severity": "hard",
      "re": "xox[baprs]-[A-Za-z0-9-]{10,}"
    },
    {
      "name": "private-key-block",
      "severity": "hard",
      "re": "-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----"
    },
    {
      "name": "generic-assignment",
      "severity": "hard",
      "re": "(api[_-]?key|secret|token|password|passwd|bearer)\\s*[:=]\\s*['\"][^'\"]{8,}",
      "flags": "i"
    }
  ],
  "trackedFileBlocklist": {
    "_note": "Env leakage — a hard block on the file being TRACKED at all, independent of contents.",
    "severity": "hard",
    "globs": [
      ".env",
      ".env.*",
      "*.pem",
      "id_rsa",
      "id_dsa",
      "id_ecdsa",
      "id_ed25519"
    ]
  },
  "skipPathsContaining": {
    "_note": "Paths whose ENTIRE purpose is to contain pattern-shaped text. Matched as a path SUFFIX (2026-09-23), so the same files are skipped wherever the repo puts secure-push - the vault tree or the published slice. Distinct from the baseline: the baseline accepts specific known matches, this skips files where scanning is meaningless by construction. Keep this list short - every entry is a blind spot, per instrument-blind-to-the-fault-class.",
    "values": [
      "secure-push/scripts/credential-patterns.json",
      "secure-push/scripts/credential-baseline.json",
      "secure-push/scripts/public-patterns.json",
      "secure-push/scripts/public-denylist.example.json",
      "secure-push/scripts/credential-scan.test.mjs"
    ]
  }
}
